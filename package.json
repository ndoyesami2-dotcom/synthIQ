{
  "name": "synthiq",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20 <25"
  },
  "dependencies": {
    "firebase-admin": "^13.8.0",
    "stripe": "^22.1.0"
  },
  "scripts": {
    "start": "node server.cjs",
    "test": "firebase emulators:exec --only firestore --project demo-synthiq \"vitest run\"",
    "test:watch": "firebase emulators:exec --only firestore --project demo-synthiq \"vitest\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.0.0",
    "vitest": "^2.1.4"
  }
}
